let country = "Portugal";
let continent = "Europe";
let population = 10;

console.log(country);
console.log(continent);
console.log(population);

// recreate the fullMessage variable using the template literal syntax.

let fullMessage = `${country} is based in ${continent} with over ${population} million people.`;

console.log(fullMessage);
